#pragma once
#include <cstdint>
#include <string>

struct Bar {
    std::string epic;
    std::string resolution;
    int64_t timestamp;
    double open;
    double high;
    double low;
    double close;
    double volume;
};
